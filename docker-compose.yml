version: '3.8'
services:
  db:
    image: postgres
    container_name: efcore-postgres
    environment:
      POSTGRES_DB: labrestapi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: lab
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
